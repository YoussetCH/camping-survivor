--!strict

export type StructureDefinition = {
	id: string,
	gridWidth: number,
	gridDepth: number,
	maxHp: number?,
	station: string?,
	modelSize: Vector3,
	modelColor: Color3,
	recipeId: string?,
}

return {}
